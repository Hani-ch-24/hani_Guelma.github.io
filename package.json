{
  "name": "my-react-app",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://YOUR_GITHUB_USERNAME.github.io/REPO_NAME", // ← Must match your repo
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",  // ← Builds the app
    "deploy": "gh-pages -d build", // ← Deploys the `build` folder
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0" // ← Required for GitHub Pages
  }
}
